- ssh 
- go to projects directory in $SCRATCH
- run tmux
- request for gpu using: srun --cpus-per-task=8 --mem=64GB --time=12:00:00 --gres=gpu:1 --pty --account=pr_338_general /bin/bash
- ssh into the node (if needed after being allocated)
- setup virtual env, install libraries
- run the file
-  --account=pr_338_general
- to create and activate venv:
    python3 -m venv ./venv_seld
    source ./venv_seld/bin/activate

# Remove old key
ssh-keygen -R greene.hpc.nyu.edu

# To run python file if everything is set up
in the folder - run:
  ./sing -> sets up everything and the singularity container
  then activate venv
  then run python file


To copy files from scratch to local machine:
  scp -r nd2746@greene.hpc.nyu.edu:/scratch/nd2746/seld_project/logs .

To copy files from local machine to scratch:
  scp smrl_seld.py nd2746@greene.hpc.nyu.edu:/scratch/nd2746/seld_project/